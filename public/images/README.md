`kukudhamini-logo.png` is the canonical PNG copy of the provided KukuDhamini
artwork. The original supplied raster is retained as `kukudhamini-logo.png.jpg`.

The application uses the canonical artwork for the sidebar, mobile header, and
authentication pages. The generated files in `public/icons` preserve the same
artwork at the required PWA sizes. Do not replace any of these files with a
redraw, simplification, or unrelated icon.